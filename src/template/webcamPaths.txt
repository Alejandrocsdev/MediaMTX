  {{RTSP_PATH}}:
    runOnInit: >
      ffmpeg -f v4l2 -input_format {{FORMAT}} -framerate 30 -i {{WEBCAM}} -vcodec libx264 -preset veryfast -tune zerolatency -pix_fmt yuv420p -f rtsp rtsp://{{RTSP_HOST}}:{{RTSP_PORT}}/{{RTSP_PATH}}
    runOnInitRestart: yes